## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  ## One-Wire Bus Component (RMT-based hardware timing)
  espressif/onewire_bus: ^1.0.4
  ## ADS1x15 ADC Component (ADS1015/ADS1115 I2C ADC support)
  espp/ads1x15: ^1.0.28
  ## BME280 Environmental Sensor Component (Temperature, Humidity, Pressure)
  ## Note: Component may need to be added as a submodule or local component
  ## If component registry doesn't work, we can implement direct register access
  ## For now, commenting out - will implement alternative approach if needed
  # bme280: ^2.0.0

